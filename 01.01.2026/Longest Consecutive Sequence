class Solution:
    def longestConsecutive(self, nums):
        s, ans = set(nums), 0
        for n in s:
            if n-1 not in s:
                cur = n
                while cur in s:
                    cur += 1
                ans = max(ans, cur-n)
        return ans
