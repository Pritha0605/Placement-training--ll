def hIndex(citations):
    l, r = 0, len(citations)-1
    n = len(citations)
    while l <= r:
        m = (l+r)//2
        if citations[m] >= n-m:
            r = m-1
        else:
            l = m+1
    return n-l
