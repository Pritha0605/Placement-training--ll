class Solution {
    public int hIndex(int[] c) {
        int l=0, r=c.length-1, n=c.length;
        while(l<=r){
            int m = l + (r-l)/2;
            if(c[m] >= n-m) r = m-1;
            else l = m+1;
        }
        return n - l;
    }
}
