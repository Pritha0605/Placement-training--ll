def getFactors(n):
    res = []
    def backtrack(start, n, path):
        if path:
            res.append(path + [n])
        for i in range(start, int(n**0.5)+1):
            if n % i == 0:
                backtrack(i, n//i, path+[i])
    backtrack(2, n, [])
    return res
